# 计划（PLAN）

## 目标

## 里程碑

## 设计（architect 填写）

### 模块边界

### 接口契约（I/O 用 JSON Schema，精确到字段/类型/错误语义）

```json
{
  "接口名": {
    "input": { "字段": "类型与约束" },
    "output": { "字段": "类型与约束" },
    "errors": { "错误码": "语义" }
  }
}
```

### 数据流

### 技术选型与理由

### 风险与取舍
