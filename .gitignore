# ── 编辑器 / 系统噪音 ─────────────────────────────────────────────────────
.DS_Store
*.bak
*.bak-*
*.tsbuildinfo
__pycache__/

# ── 依赖（本包零运行时依赖；出现即说明是本地调试留下的）──────────────────
node_modules/

# ── 运行时产物 ───────────────────────────────────────────────────────────
# `/team` 会往 **cwd** 写 team/<run>/；若在包目录内跑过 /team，这里挡住误提交。
# 注意：$DSH_HOME 下的 skill/preset/运行副本不在本仓内，由 gate:sync 负责一致性。
team/

# ── 本地发布凭据（绝不入库）──────────────────────────────────────────────
.npmrc
*.tgz
